#include <bits/stdc++.h>
using namespace std;
#define print(x) cout << x << endl;
#define input(x) cin >> x;

int divisor(int num)
{
    vector<int> v;
}

int main()
{
    int a, b, c;
    input(a);
    input(b);
    input(c);

    return 0;
}
