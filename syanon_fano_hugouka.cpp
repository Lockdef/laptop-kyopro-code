#include <bits/stdc++.h>
using namespace std;
#define print(x) cout << x << endl;
#define rep(i, n) for (int i = 0; i < n; ++i)
using ll = long long int;

int f(int a)
{
    vector<int> v;
    for (int a)
}

int main()
{
    int n; // 記号の数
    map<int, double> a;

    for
        rep(i, n)
        {
            cin << a[n];
        }

    map<int, string> r;

    return 0;
}
